<% provide(:title, 'All users') %>

<div class="container">

  <h1>All users (<%= User.count %>)</h1>

  <%= will_paginate %>

  <ul class="users">
  	<%= render @users %>
  </ul>

  <%= will_paginate %>

</div>
